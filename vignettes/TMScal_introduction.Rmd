---
title: "TMScal Introduction"
author: "Xue Cheng, Yutai Xue"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{TMScal Introduction}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

# TMScal: Tumor Mutational Signature Calculator

## Overview
TMScal predicts prognostic risks using Tumor Mutational Spectrum (TMS) 
from WES data of MSS cancer patients.

## Quick Start with Example Data
```{r, eval=FALSE}
library(TMScal)

# Get example data paths
maf_train <- system.file("extdata", "data_mutations_train_70.txt", package = "TMScal")
clin_train <- system.file("extdata", "data_clinical_train_70.txt", package = "TMScal")

# Train model
model <- train(maf_train, clin_train, output_dir = "./model")

# Summary
summary(model)

# Predict
maf_test <- system.file("extdata", "data_mutations_test_30.txt", package = "TMScal")
predictions <- predict(maf_test, model)

# KM curve
plot_km_curve("./predictions.csv", 
              system.file("extdata", "data_clinical_test_30.txt", package = "TMScal"))
```

##Methodology
- Extract 192 trinucleotide mutational pathways
- CLR transformation of pathway proportions
- Bootstrap-based stability assessment (200 iterations)
- Univariate Cox regression for prognostic value
- Feature selection: stability < 0.07 + adaptive P-value cutoff
- 60:40 stratified train/test split
- TMS score = weighted sum of selected pathway CLR values
- Tertile-based risk stratification (Low/Medium/High)


## LICENSE：
  Apache License (>= 2.0) + file LICENSE

