# Example Data

Place example data files here for testing:

- `example_mutations.txt` - MAF format with 7 columns:
  chromosome, start_pos, ref_allele, alt_allele, sample_id, variant_class, context

- `example_clinical.txt` - Clinical data with 3 columns:
  sample_id, os_time, os_event